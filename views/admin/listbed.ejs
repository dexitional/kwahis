		<script>
			// Messages on Records 
		    <% if(msg === 'saved'){ %>
				  toastr.success('', 'Record Saved Successfully!');					 
			<% }%>

			<% if(msg === 'unassigned'){ %>
				  toastr.success('', 'Bed Un-assigned Successfully!');					 
			<% }%>

            <% if(msg === 'genbeds'){ %>
				  toastr.success('', 'Bed Slots Generated Successfully!');					 
			<% }%>
			
			
			
			// JQuery Logics
			 $(function(){               
		
		

			 });

		</script>



		<div class="row clearfix">

			  <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
				  <div class="card">                            
					  <div class="header">
						  <h3><i class="fa fa-pay"></i> ASSIGNED BEDS  |  ( <%= academ.name %> ACADEMIC YEAR, SEMESTER <%= academ.semester %> )</h3>						
					  </div>
					  <div class="body">
						 <h5><a class="btn btn-primary" href="/bed/generate" title="Generate Bed Slots for Semester" onclick="if(!confirm('Generate Bed Slots for Semester?'))return false;"><i class="fa fa-plus"></i> Generate Bed Slots</a></h5>
				    	 <div class="table-responsive">
							  
							  <table class="table table-bordered table-striped table-hover dataTable js-exportable">
								  <thead>
									  <tr role="row">
										  <th>#</th>
										  <th>Room</th>
										  <th>Bed No.</th>
										  <th>Mattress No.</th>
										  <th>Position</th>
										  <th>Assigned User</th>
										  <th>Group</th>
										  <th>Gender</th>
										  <th>&nbsp;</th>
									  </tr>
								  </thead>								  
								  <tbody> 
									
									<% rows.map((row,i) => { %>
									  <tr class="odd">
											<td><%- (i+1) %></td>
											<td><b><%= row.room %></b></td>
											<td><b><%- row.indexes %><b></td>
											<td><b><%- row.mattress %><b></td>
											<td><b><%- (row.indexes.substring(0,1) == 'D' ? 'DOWN BED':'TOP BED') %></b></td>	
											<td><b><%- row.student %></b></td>	
											<td><b><%- row.gname %></b></td>
											<td><b><%- row.gender == 'M' ? 'MALE' : (row.gender == 'F' ? 'FEMALE':'') %></b></td>											
											<td>
												<% if(row.user !== null){%>
												<div class="btn-group">
													<a href="/bed/unassign/<%- row.id %>" title="Un-assign Bed" style="margin-right:5px;" class="btn btn-danger btn-sm"  onclick="return confirm('Un-assign Bed?')"><small><b>UN-ASSIGN</b></small></a>
													<a href="/bed/unassign/<%- row.id %>" title="Un-assign Bed" style="margin-right:5px;" class="btn btn-success btn-sm"  onclick="return confirm('Un-assign Bed?')"><small><b><i class="fa fa-plus"></i> MAT</b></small></a>
												</div>
											    <% }%>
											</td>                                                                                         
									   </tr>
									<% }) %>
								  </tbody>
							   </table>
							  
						  </div>
					  </div>
				  </div>
			  </div>
		  </div>